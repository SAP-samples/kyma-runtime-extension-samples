spring:
  datasource:
    url: jdbc:h2:mem:testdb
  jpa:
    show-sql: true
    hibernate:
      dialect: org.hibernate.dialect.H2Dialect
application:
  cluster.domain: "localhost:8080"
  subdomain: ""
  is.secure: false
apiVersion: gateway.kyma-project.io/v1alpha1
kind: APIRule
metadata:
  name: hanadb-nodejs
spec:
  gateway: kyma-gateway.kyma-system.svc.cluster.local
  rules:
  - accessStrategies:
    - config: {}
      handler: noop
    methods:
    - GET
    - POST
    - PUT
    - PATCH
    - DELETE
    - HEAD
    path: /.*
  service:
    host: hanadb-nodejs
    name: hanadb-nodejs
    port: 3000
